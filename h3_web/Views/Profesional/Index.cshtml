@model IEnumerable<h3_web.Models.Profesional>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Horario1.Especialidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona.Documento)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Horario1.Especialidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Persona.Documento)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Doc_Pro }) |
                @Html.ActionLink("Details", "Details", new { id=item.Doc_Pro }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Doc_Pro })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
